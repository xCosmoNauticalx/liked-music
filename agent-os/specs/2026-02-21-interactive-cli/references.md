# References for Interactive CLI

## Config Wizard

- **Location:** `likedmusic/config_wizard.py`
- **Relevance:** Existing interactive flow using questionary + rich
- **Key patterns:** `_ask()` helper for Ctrl+C handling, `questionary.select`/`checkbox`/`confirm`, rich Console + Panel + Table
