# Backup Restructure — Plan

Replace global sync_state.json with per-playlist .json files in the backup directory, flatten audio backups into a single Backup/ folder, and add SHA256 checksum + .bak recovery for corruption protection.

See the full plan at: `.claude/plans/validated-launching-spring.md`
